# devops-dashboard

A small Python CLI tool for basic DevOps-style utilities.

## Modules
- sysinfo — displays hostname, current user, and current working directory
- logcheck — reads a log file and prints lines containing ERROR
- tasklist — manages a simple in-memory task list
